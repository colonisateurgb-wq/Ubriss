[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Server-rendered routes (API routes, dashboard) run as Netlify Functions
# automatically via the Next.js plugin above — no extra redirects needed.
